export interface LoginResponse {
  name: string;
  dpi: string;
  token: string;
  redirect: string;
  userId: number;
  employeeCode: string;
  companyCode: number;
  companyName: string;
  countryId: number;
  countryName: string;
  startYearToWork: number;
  startDateToWork: string;
}
