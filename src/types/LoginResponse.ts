export interface LoginResponse {
  name: string;
  dpi: string;
  token: string;
  redirect: string;
  userId: number;
  employeeCode: string;
  companyCode: number;
  startYearToWork: number;
  startDateToWork: string;
}
